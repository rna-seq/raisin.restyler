= raisin.restyler =

This package contains the models used to render the pages of repoze.bfg.

* The Page class in page.py

    * Fetches all resources needed for a page and renders them

* The Box class in box.py

    * Fetches the resource for the box and renders it in the different formats
